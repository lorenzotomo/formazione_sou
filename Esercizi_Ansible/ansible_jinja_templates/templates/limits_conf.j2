# Configurazione limiti generata da Ansible
# Utente: {{ target_user }}
# Ambiente: {{ env_type }}

{% if env_type == 'production' %}
{{ target_user }} hard nofile 10000
{{ target_user }} soft nofile 10000
{% else %}
{{ target_user }} hard nofile 1000
{{ target_user }} soft nofile 1000
{% endif %}