apiVersion: v1
kind: Service
metadata:
  name: web-dev-service
  labels:
    environment: dev
    app: web
    region: EU
spec:
  type: LoadBalancer
  selector:
    app: web
    environment: dev
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
